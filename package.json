{
  "name": "bro",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/gen-embedded.ts && node scripts/gen-plugins.ts && nx run-many -t build --exclude=skills-*,plugins-*-skills-*",
    "check:embedded": "node scripts/gen-embedded.ts --check",
    "gen:plugins": "node scripts/gen-plugins.ts",
    "check:plugins": "node scripts/gen-plugins.ts --check",
    "test": "nx run-many -t test --exclude=skills-*,plugins-*-skills-*",
    "lint": "nx run-many -t lint",
    "affected": "nx affected -t build test lint --exclude=skills-*,plugins-*-skills-*",
    "typecheck": "nx run-many -t typecheck --exclude=skills-*,plugins-*-skills-*"
  },
  "devDependencies": {
    "@nx-devkit/skill": "^0.1.9",
    "@nx-devkit/skillspector": "^0.1.9",
    "@nx-devkit/tsdown": "^0.1.7",
    "@nx/js": "^23.2.1",
    "@types/node": "^22.0.0",
    "nx": "^23.1.0",
    "tsdown": "^0.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "overrides": {
    "smol-toml": "1.7.1"
  }
}
